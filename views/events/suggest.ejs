<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Go to Chatbot</title>
    <style>
        /* Reset margin and padding */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Basic body styles */
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        /* Container box */
        .container {
            background-color: #201e43;  /* Dark Space Blue */
            color: #ffffff;
            width: 100%;
            max-width: 400px; /* Limits the width */
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            text-align: center;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            box-sizing: border-box;
        }

        /* Improved statement */
        .statement {
            font-size: 1.1rem;
            margin-bottom: 20px;
            color: #eeeeee;
        }

        /* Button styling */
        button a {
            background-color: #134b70; /* Deep Ocean Blue */
            color: #f5f5f5;
            font-size: 1rem;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            text-decoration: n;
        }

        button a:hover {
            background-color: #508c9b; /* Muted Teal */
        }

        /* Responsive design */
        @media (max-width: 600px) {
            .container {
                width: 90%; /* Makes it smaller on mobile */
                padding: 15px;
            }

            .statement {
                font-size: 1rem;
            }

            button a{
                font-size: 0.9rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <p class="statement">Book your shows or events effortlessly through the chatbot, available in the rightmost corner of the home page. </p>
        <button ><a href="/">Go to Home Page</a></button>
    </div>
</body>
</html>
